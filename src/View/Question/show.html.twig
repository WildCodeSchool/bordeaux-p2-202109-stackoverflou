{% extends 'layout.html.twig' %}

{% block content %}
    <h1>{{ question.title }}</h1>
    {% include 'Components/_tag.html.twig' %}
    <h2>Description</h2>
    <p>{{ question.description|nl2br }}</p>
    {% if session.user.is_admin %}
    <a href="/questions/edit?id={{ question.id }}">Edit</a>
    {% endif %}
    <a class="btn btn-primary" href="/questions/answers?id={{ question.id }}">Répondre</a>

    <a href="/questions">Retour</a>
{% endblock %}
